# AIOps 智能运维平台 — 全新服务器部署手册（含安全加固）

> 适用场景：医院内外网服务器（含完全离线内网环境），在一台**全新服务器**上从零搭建。
> 部署包：`aiops-deploy.zip`（已含全部离线依赖 + 便携 Python，目标机无需联网）。
> 当前版本包含：服务器/VMware/交换机(端口明细)/存储(SNMP+SMI-S)/温湿度监控、告警中心、
> 机柜管理(3D视图+Excel导入)、AI助手(千问)。

---

## 一、部署包内容说明

```
aiops-deploy/
├── backend/                        # 后端程序 (FastAPI + SQLite)
│   ├── app/                        #   后端源码
│   ├── packages/                   #   Agent安装包(内置便携Python, 零依赖)
│   │   ├── aiops-agent-linux.tar.gz
│   │   └── aiops-agent-windows.zip
│   ├── requirements.txt            #   后端依赖清单
│   └── .env.example                #   配置模板(复制改名为 .env 使用)
├── frontend-dist/                  # 前端构建产物 (Nginx 直接指向本目录)
├── agent/                          # Agent 源码与安装脚本 (参考用)
├── packages/
│   ├── linux/                      #   Linux 离线依赖 (Python 3.9~3.13 wheel)
│   ├── windows/                    #   Windows 离线依赖 (Python 3.11 wheel)
│   └── python-3.11.9-amd64.exe     #   Windows Python 安装包
├── scripts/
│   ├── install_backend_linux.sh    #   Linux 后端一键安装 (systemd 自启)
│   ├── install_backend_windows.bat #   Windows 后端一键安装
│   ├── start_backend_windows.bat   #   Windows 后端启动
│   ├── update.sh                   #   版本升级脚本 (保留数据库与配置)
│   └── nginx-aiops.conf            #   Nginx 配置模板
└── 医院环境部署手册.md              # 本文档
```

---

## 二、部署架构与端口规划

```
                     ┌──────────────────────────┐
  运维人员浏览器 ────→ │  Nginx (80)              │
                     │  前端页面 + /api 反代      │
                     └────────────┬─────────────┘
                                  │
                     ┌────────────▼─────────────┐
                     │  FastAPI 后端 (8080)      │
                     │  SQLite: backend/aiops.db │
                     └────────────┬─────────────┘
        ┌──────────────┬──────────┼──────────┬─────────────┐
   Agent上报(5s)   vCenter443   SNMP      SMI-S 5989    千问LLM
```

**端口清单（平台服务器）：**

| 端口 | 协议 | 用途 | 放行来源 |
|------|------|------|---------|
| 80 | TCP | Web界面(Nginx) | 运维网段 |
| 8080 | TCP | 后端API + Agent上报 + Agent安装脚本下载 | 运维网段 + 所有被监控服务器网段 |
| 161/UDP | 出方向 | 平台 → 交换机/存储 SNMP采集 | - |
| 5989 | TCP | 平台 → 存储 SMI-S采集(EMC/戴尔) | - |
| 443 | 出方向 | 平台 → vCenter | - |
| 8000/11434 | 出方向 | 平台 → 千问大模型(vLLM/Ollama) | - |

**服务器要求：**

| 角色 | 建议配置 | 系统 |
|------|---------|------|
| 平台服务器 | 4核8G / 100G盘 (监控数据存3个月) | CentOS/Rocky/openEuler/龙蜥/Ubuntu 或 Windows Server |
| 被监控服务器 | 无要求 | 任意（Agent 零依赖，无需装Python） |

> 时区提醒：平台按 Asia/Shanghai 显示时间，新服务器请确认时间正确：
> `timedatectl set-timezone Asia/Shanghai`

---

## 三、全新服务器部署步骤（Linux 方案，推荐）

以下以部署目录 `/opt/aiops-deploy` 为例，**复制粘贴执行即可**。

### 第1步：上传并解压部署包

用已审批的U盘/光盘/scp 将 `aiops-deploy.zip` 拷入服务器：

```bash
cd /opt
unzip -o /tmp/aiops-deploy.zip -d /opt      # 解压后得到 /opt/aiops-deploy
cd /opt/aiops-deploy
chmod +x scripts/*.sh
```

### 第2步：确认 Python3（3.9 ~ 3.13 均可）

```bash
python3 --version
```

- 有输出且版本在 3.9~3.13 之间 → 直接进第3步。
- 无 python3：需先用系统安装源装一次（仅装解释器，依赖全是离线的）：

```bash
# CentOS / Rocky / openEuler / 龙蜥（如本地源可用）
yum install -y python3 python3-pip
# Ubuntu / Debian
apt install -y python3 python3-venv
```

> 离线且连 python3 都装不了？把平台装到一台有 python3 的机器，
> 或联系开发者按目标系统重新打包（可内置便携 Python 运行时）。

### 第3步：一键安装后端

```bash
cd /opt/aiops-deploy
./scripts/install_backend_linux.sh
```

脚本自动完成：创建 venv → 离线装依赖 → 注册 `aiops-backend` systemd 服务并设开机自启。

### 第4步：写配置文件（安全必改项）

```bash
cd /opt/aiops-deploy/backend
cp .env.example .env
vi .env
```

**必须修改的两项：**

```ini
# 生成随机密钥: openssl rand -hex 32
SECRET_KEY=<替换为随机串>
# Agent上报令牌(所有Agent配置需与此一致, 防止伪造上报)
AGENT_TOKEN=<替换为自定义令牌>
```

**接入千问大模型时修改（可选，不影响其他功能）：**

```ini
LLM_BASE_URL=http://大模型服务器IP:8000/v1   # vLLM
# 或 Ollama: http://大模型服务器IP:11434/v1
LLM_MODEL=qwen2.5
```

### 第5步：启动并验证后端

```bash
systemctl start aiops-backend
systemctl status aiops-backend        # active (running) 即成功
curl http://127.0.0.1:8080/api/health # 返回 {"status":"ok"}
```

### 第6步：安装并配置 Nginx（前端）

```bash
# 在线环境:
yum install -y nginx        # CentOS/Rocky/openEuler/龙蜥
# apt install -y nginx      # Ubuntu
# 完全离线环境: 挂载系统ISO配本地源后执行同样命令, nginx在基础源里

# 配置站点（root 已指向部署包内 frontend-dist, 无需拷贝文件）
cp /opt/aiops-deploy/scripts/nginx-aiops.conf /etc/nginx/conf.d/aiops.conf
vi /etc/nginx/conf.d/aiops.conf   # 仅需确认/修改 server_name, 默认 _ 全匹配

nginx -t                           # syntax ok
systemctl enable --now nginx
```

> 若系统 nginx 没有加载 conf.d，检查 `/etc/nginx/nginx.conf` 的 http{} 中
> 是否有 `include /etc/nginx/conf.d/*.conf;`。
> 若与默认站点冲突，删除 `/etc/nginx/conf.d/default.conf` 或注释其 server 块。

### 第7步：防火墙放行

```bash
firewall-cmd --permanent --add-port=80/tcp
firewall-cmd --permanent --add-port=8080/tcp
firewall-cmd --reload
```

### 第8步：首次登录与安全加固

浏览器访问 `http://平台IP/`，默认账号 **admin / admin123**。

**立即执行（重要）：**
1. 修改 admin 密码（页面内「修改密码」入口）
2. 确认第4步已改 `SECRET_KEY` 和 `AGENT_TOKEN`
3. 防火墙对 8080 仅放行被监控服务器网段（用户浏览器只需 80）

---

## 四、全新服务器部署步骤（Windows 方案）

1. **装 Python**：双击 `packages\python-3.11.9-amd64.exe`，**勾选 Add Python to PATH**
2. **装后端**：双击 `scripts\install_backend_windows.bat`
3. **写配置**：复制 `backend\.env.example` 改名为 `backend\.env`（记事本编辑，必改 SECRET_KEY / AGENT_TOKEN）
4. **启动**：双击 `scripts\start_backend_windows.bat`
   - 建议用 NSSM 注册为服务实现开机自启：
     ```
     nssm install AIOpsBackend "C:\aiops-deploy\backend\venv\Scripts\python.exe" "-m uvicorn app.main:app --host 0.0.0.0 --port 8080"
     nssm set AIOpsBackend AppDirectory "C:\aiops-deploy\backend"
     nssm start AIOpsBackend
     ```
5. **前端**：安装 Nginx for Windows，把 `scripts\nginx-aiops.conf` 内容按实际路径修改后引入
   （root 改为 `C:/aiops-deploy/frontend-dist`）
6. **防火墙**：入站规则放行 80、8080

---

## 五、被监控服务器部署 Agent（零依赖，免装 Python）

> 前提：平台后端已启动。命令中 `172.16.10.147` 换成新平台服务器IP。
> 登录平台 → 「Agent安装」页面可自动生成命令并下载安装包。

### Linux（CentOS/openEuler/Rocky/Ubuntu/龙蜥，一行命令）

```bash
curl -sSfL 'http://<平台IP>/api/agents/install.sh' | sudo bash -s -- --server 'http://<平台IP>'
```

自动完成：下载内置便携Python的安装包 → 解压到 /opt/aiops-agent → 写配置 → systemd 开机自启 → 启动。

**完全离线的机器**：从平台下载 `http://<平台IP>/api/agents/package/linux` 拷到目标机：

```bash
tar xzf aiops-agent-linux.tar.gz -C /opt/aiops-agent --strip-components=1 2>/dev/null \
  || (mkdir -p /opt/aiops-agent && tar xzf aiops-agent-linux.tar.gz -C /opt/aiops-agent)
bash /opt/aiops-agent/install.sh --server 'http://<平台IP>'
```

### Windows（2008/2012/2016/2019）

1. 下载 `http://<平台IP>/api/agents/package/windows`
2. 解压到任意目录（如 `C:\aiops-agent`），双击 **install.bat**
3. 按提示输入平台地址，回车即完成（计划任务开机自启）

### 验证与卸载

```bash
systemctl status aiops-agent     # Linux; 平台"服务器管理"几秒内看到上线
# 卸载 Linux:
systemctl disable --now aiops-agent && rm -rf /opt/aiops-agent /etc/systemd/system/aiops-agent.service
# 卸载 Windows:
schtasks /delete /tn "AIOpsAgent" /f   再删除安装目录
```

> **改过 AGENT_TOKEN 的**：需同步修改每台 Agent 的
> `/opt/aiops-agent/config.yaml` 中 `token` 字段后 `systemctl restart aiops-agent`。

---

## 六、监控对象接入

### 6.1 VMware vSphere
「VMware管理」→ 添加 vCenter（地址/账号/密码）→ 点「同步」。
自动拉取宿主机+虚拟机，CPU/内存实时使用率每5分钟同步。

### 6.2 交换机（华为/华三/Dell 等）
先在交换机开只读 SNMP：
```
system-view
snmp-agent community read <你的community>
snmp-agent sys-info version v2c
```
「网络设备」→ 添加（IP + community）→ 点「SNMP轮询」。
自动采集：CPU/内存、端口列表与状态（UP/DOWN/速率），每2分钟自动刷新。
端口明细弹窗内可逐端口「测试」和编辑备注（如“到3F病房接入”）。

### 6.3 存储设备（华为/华三/宏杉/EMC 等）
「存储设备」→ 添加，按设备类型选择协议：

| 协议 | 适用 | 需填写的凭据 |
|------|------|------------|
| SNMP | 华为OceanStor、华三、宏杉及大多数支持SNMP的存储 | community / v2c |
| SMI-S | EMC/Dell EMC、部分支持CIM的阵列 | 管理IP、账号、密码（EMC默认命名空间 root/emc） |

采集内容：容量使用、控制器状态、健康状态；SNMP需在存储侧开启只读SNMP服务。

### 6.4 温湿度传感器
「温湿度监控」→ 添加传感器。支持HTTP推送的网关配置其定时调用：
`POST http://<平台IP>:8080/api/env/<传感器ID>/push?temperature=25.5&humidity=45`

### 6.5 千问大模型（内网）
`backend/.env` 中修改 `LLM_BASE_URL / LLM_API_KEY / LLM_MODEL`，
重启：`systemctl restart aiops-backend`。
也可在平台「AI助手」→ 设置界面在线配置并测试连通。

### 6.6 机柜管理
「机柜管理」→ 新建机柜（自定义编号、列名、U数默认42可改）→
点击机柜打开3D视图/U位图 → 添加设备（服务器/交换机/存储/安全设备等，
可选**前侧/后侧**放置，同U位前后可各放一台）。
**Excel批量导入**：页面点「下载导入模板」→ 按模板填写 → 「导入设备」→
预览确认。机柜编号不存在会自动建柜。

---

## 七、日常运维

```bash
# 服务管理
systemctl start|stop|restart aiops-backend
journalctl -u aiops-backend -f          # 后端日志
journalctl -u aiops-agent -f            # Agent日志(在被监控机上)

# 数据库备份（SQLite 单文件, 建议每日定时）
cp /opt/aiops-deploy/backend/aiops.db /备份目录/aiops_$(date +%F).db

# crontab 每天凌晨2点自动备份, 保留14天
echo '0 2 * * * cp /opt/aiops-deploy/backend/aiops.db /opt/dbbak/aiops_$(date +\%F).db && find /opt/dbbak -name "aiops_*.db" -mtime +14 -delete' | crontab -

# 恢复: 停后端 → 覆盖aiops.db → 起后端
systemctl stop aiops-backend && cp 备份.db /opt/aiops-deploy/backend/aiops.db && systemctl start aiops-backend
```

- 监控指标数据**自动保留3个月**，过期自动清理。
- Agent 采集间隔默认5秒，在 Agent `config.yaml` 中可调。

---

## 八、版本升级（老平台 → 新版本）

```bash
# 新包上传 /tmp 后:
unzip -o /tmp/aiops-deploy.zip -d /tmp/aiops-new
bash /opt/aiops-deploy/scripts/update.sh /tmp/aiops-new/aiops-deploy
```

自动：备份 → 覆盖后端代码/前端/Agent包 → 安装新增离线依赖 → 重启 → 健康检查。
**保留**：数据库、.env、venv，数据不丢。浏览器 Ctrl+F5 强刷。

---

## 九、故障排查

| 现象 | 排查步骤 |
|------|---------|
| 打不开页面 / welcome to nginx | `nginx -t`；确认 conf 的 root 指向 `frontend-dist`（**无dist子目录**）且目录下有 index.html；`systemctl reload nginx` |
| 登录报 502 | 后端没起来：`systemctl status aiops-backend`；`journalctl -u aiops-backend -n 50` 看报错；修复后 `systemctl restart aiops-backend` |
| 前端白屏/样式乱 | 浏览器 Ctrl+F5 强刷（前端有升级缓存） |
| Agent 不上线 | 被监控机 `curl http://<平台IP>:8080/api/health` 测连通；核对 config.yaml 的 server 与 token；`systemctl status aiops-agent` |
| 交换机 CPU/内存为0、端口采不到 | 核对 community/版本；在交换机上 `display snmp-agent community`；平台点「SNMP轮询」重试 |
| 存储采不到数据 | SNMP类:确认存储已开SNMP只读服务；SMI-S类:确认5989端口可达、账号密码正确 |
| AI助手无响应 | `curl http://<LLM地址>/v1/models` 验证模型服务；确认 .env 后已重启后端 |
| 时间差8小时 | `timedatectl set-timezone Asia/Shanghai` 并重启后端 |

---

## 十、安全加固清单（医院环境必读）

| # | 加固项 | 操作 |
|---|--------|------|
| 1 | 修改默认口令 | 首次登录立即改 admin 密码，密码≥8位含大小写数字 |
| 2 | 更换签名密钥 | `.env` 的 `SECRET_KEY` 用 `openssl rand -hex 32` 生成 |
| 3 | 更换Agent令牌 | `.env` 的 `AGENT_TOKEN` 自定义，并同步所有Agent |
| 4 | 网络隔离 | 平台只对内网开放，**严禁映射到外网**；8080仅对被监控网段放行 |
| 5 | 最小化放行 | 防火墙按第二章端口表配置，不出网方向仅放行vCenter/SNMP/LLM |
| 6 | 数据库保护 | `chmod 600 /opt/aiops-deploy/backend/aiops.db`；每日异地备份 |
| 7 | HTTPS(可选) | 如需加密传输，为nginx配证书并将 80 重定向 443 |
| 8 | 系统加固 | 及时安装系统安全补丁；SSH改密钥登录；时间同步 chrony |
| 9 | 审计 | 定期 `journalctl -u aiops-backend --since "-7d" \| grep -i error` 巡检 |
| 10 | 人员离职 | 及时修改admin密码与AGENT_TOKEN |

---

## 附：快速命令汇总（全新Linux服务器，10分钟上线）

```bash
cd /opt && unzip -o /tmp/aiops-deploy.zip -d /opt && cd aiops-deploy && chmod +x scripts/*.sh
./scripts/install_backend_linux.sh
cd backend && cp .env.example .env && vi .env     # 改 SECRET_KEY / AGENT_TOKEN
systemctl start aiops-backend && curl http://127.0.0.1:8080/api/health
yum install -y nginx && cp ../scripts/nginx-aiops.conf /etc/nginx/conf.d/ && nginx -t && systemctl enable --now nginx
firewall-cmd --permanent --add-port=80/tcp --add-port=8080/tcp && firewall-cmd --reload
# 浏览器登录 admin/admin123 → 立即改密 → 接入设备
```
